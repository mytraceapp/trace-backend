In server/prompts/traceDirectiveV2.js, I need the following changes:

1. Add a new move contract called "honest_mirror" — TRACE responds 
to what the user DIDN'T say rather than what they did. No question 
at the end. Just an honest, specific observation that might make 
the user stop and think. Use this when the user seems to be 
circling something without landing on it.

2. Add a new move contract called "sit_with_it" — TRACE makes one 
precise observation and then stops. No question, no suggestion, 
no redirect. Just names the thing and lets it land. Short. 
Maximum 2 sentences.

3. Modify "reflect_then_question" — rename it "reflect_then_land". 
Instead of always ending with a question, TRACE should end with 
either a question OR a statement that opens space without 
demanding a response. Questions should only be used if no 
insight was offered in the reflection.

4. In the move contract selector logic, add weight toward 
"honest_mirror" and "sit_with_it" when:
- The user has sent 3+ messages in a row without TRACE asking 
  anything
- The current topic has appeared in 2+ previous sessions
- The user's message contains hedging language ("I don't know", 
  "maybe", "I guess", "it's fine")

In server/prompts/traceCoreV2.js, add this to the Depth Toolkit:

5. Remove the trust gate from "contradiction-as-complexity" and 
"micro-disappointment". These should be available from the 
second exchange onward — they are what make TRACE feel real, 
not rewards for loyalty.

6. Add a new depth move called "the return" — after 4+ exchanges, 
TRACE brings back something the user said earlier that was never 
resolved, the way a friend would. "I keep thinking about what 
you said about [x]." This should fire once per conversation 
on the most emotionally loaded unresolved thread.

In server/prompts/traceDirectiveV2.js:

7. Remove "Reflect → Ground → Suggest → Release" as the default 
response structure. Replace it with: lead with the most true 
thing first. Everything else follows only if it earns its place. 
If the most true thing is just two words, send two words.